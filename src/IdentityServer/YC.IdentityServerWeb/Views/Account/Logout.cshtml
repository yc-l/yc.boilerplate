@model LogoutViewModel

<div class="logout-page">
    <div class="lead">
        <h1>退出登录</h1>
        <p>是否要退出登录IdentityServer？</p>
    </div>

    <form asp-action="Logout">
        <input type="hidden" name="logoutId" value="@Model.LogoutId" />
        <div class="form-group">
            <button class="btn btn-primary">确定</button>
        </div>
    </form>
</div>